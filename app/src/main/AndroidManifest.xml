<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <!-- Permissions for notifications and background services -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name="com.wellsync.app.WellSyncApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WellSync.Splash"
        tools:targetApi="31">

        <!-- Onboarding Activity - NEW LAUNCHER -->
        <activity
            android:name="com.wellsync.app.OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.WellSync"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity - NEW LAUNCHER -->
        <activity
            android:name="com.wellsync.app.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.WellSync"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sign Up Activity -->
        <activity
            android:name="com.wellsync.app.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.WellSync"
            android:screenOrientation="portrait" />

        <!-- Main Activity with Splash Screen -->
        <activity
            android:name="com.wellsync.app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WellSync.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Hydration Reminder Service -->
        <service
            android:name="com.wellsync.app.services.HydrationReminderService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="shortService"
            android:stopWithTask="false"/>

        <!-- Boot Receiver for persistent reminders -->
        <receiver
            android:name="com.wellsync.app.receivers.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>
        <!-- Widget Receiver -->
        <receiver
            android:name="com.wellsync.app.widget.WellSyncWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>



    </application>

</manifest>